{
  "name": "functional-programming-lectures",
  "version": "1.0.0",
  "description": "A comprehensive curriculum for mastering functional programming with TypeScript and Redux",
  "scripts": {
    "lint": "markdownlint '**/*.md' --ignore node_modules --ignore _site",
    "lint:fix": "markdownlint '**/*.md' --ignore node_modules --ignore _site --fix",
    "check:curriculum": "node scripts/check-curriculum.mjs",
    "check:markdown-links": "node scripts/check-markdown-links.mjs",
    "check:external-links": "node scripts/check-external-links.mjs",
    "check-links": "npm run check:markdown-links && npm run check:external-links",
    "build:site": "bundle exec jekyll build --strict_front_matter",
    "check:site": "node scripts/check-site.mjs",
    "build": "npm run lint && npm run check:curriculum && npm run check-links && npm run build:site && npm run check:site"
  },
  "keywords": [
    "functional-programming",
    "typescript",
    "redux",
    "education"
  ],
  "author": "Sean Dinwiddie",
  "license": "CC-BY-4.0",
  "devDependencies": {
    "markdown-link-check": "^3.14.2",
    "markdownlint-cli": "^0.49.0"
  }
}
